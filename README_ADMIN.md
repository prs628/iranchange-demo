# راهنمای پنل ادمین - Admin Panel Guide

## 🎯 وضعیت فعلی

**فقط یک پورت کار می‌کند: پورت 3000**

سیستم جدید از **NextAuth.js** و **Prisma** استفاده می‌کند و دیگر نیازی به دو پورت جداگانه نیست.

## 🚀 نحوه اجرا

### 1. تنظیم دیتابیس (فقط یک بار)

```bash
# Generate Prisma Client
npm run db:generate

# Run migrations
npm run db:migrate

# Seed admin user
npm run db:seed
```

### 2. اجرای سرور

```bash
npm run dev
```

سرور روی `http://localhost:3000` اجرا می‌شود.

## 📍 آدرس‌ها

### پنل ادمین:
- **ورود ادمین**: `http://localhost:3000/admin/login`
- **داشبورد ادمین**: `http://localhost:3000/admin`
- **مدیریت کاربران**: `http://localhost:3000/admin/users`
- **گیفت کارت‌ها**: `http://localhost:3000/admin/gift-cards`

### پنل کاربران:
- **صفحه اصلی**: `http://localhost:3000/`
- **داشبورد**: `http://localhost:3000/dashboard`
- **ورود**: `http://localhost:3000/auth/login`
- **ثبت نام**: `http://localhost:3000/auth/register`

## 🔐 اطلاعات ادمین پیش‌فرض

- **ایمیل**: `admin@example.com`
- **رمز عبور**: `Admin@12345`
- **نقش**: `admin`

⚠️ **مهم**: در production حتماً رمز عبور را تغییر دهید!

## 🔒 کنترل دسترسی

سیستم از **Role-Based Access Control (RBAC)** استفاده می‌کند:

- ✅ **کاربران admin** می‌توانند به `/admin/*` دسترسی داشته باشند
- ❌ **کاربران عادی** به `/admin/*` دسترسی ندارند (redirect به `/dashboard`)
- 🔐 **کاربران لاگین نکرده** به `/admin/*` redirect می‌شوند به `/admin/login`

## 🗄️ دیتابیس

سیستم از **SQLite** برای development استفاده می‌کند:
- فایل دیتابیس: `prisma/dev.db`
- برای production از PostgreSQL یا MySQL استفاده کنید

## 📝 دستورات مفید

```bash
# اجرای سرور development
npm run dev

# Build برای production
npm run build

# اجرای production
npm run start

# دیتابیس
npm run db:generate  # Generate Prisma Client
npm run db:migrate   # Run migrations
npm run db:seed      # Seed admin user
npm run db:studio    # Open Prisma Studio (GUI)
```

## 🛠️ عیب‌یابی

### اگر پورت 3000 در حال استفاده است:

```bash
# متوقف کردن پروسه روی پورت 3000
lsof -ti:3000 | xargs kill -9
```

### اگر دیتابیس مشکل دارد:

```bash
# حذف دیتابیس قدیمی و ایجاد مجدد
rm prisma/dev.db
npm run db:migrate
npm run db:seed
```

### اگر admin user وجود ندارد:

```bash
# اجرای seed script
npm run db:seed
```

## 📚 مستندات بیشتر

- `SETUP.md` - راهنمای کامل نصب و راه‌اندازی
- `CHANGES.md` - لیست تغییرات انجام شده
- `PORTS.md` - توضیحات کامل درباره پورت‌ها

## ⚠️ نکات مهم

1. **فقط یک پورت**: دیگر نیازی به پورت 3001 نیست
2. **Route-based**: ادمین و کاربر از طریق route ها جدا می‌شوند
3. **Session-based**: احراز هویت با NextAuth و HttpOnly cookies
4. **Database**: همه کاربران در دیتابیس ذخیره می‌شوند (نه localStorage)

## 🔄 تغییرات از نسخه قدیمی

### قبل (قدیمی):
- ❌ دو پورت جداگانه (3000 و 3001)
- ❌ localStorage برای auth
- ❌ مشکل sync بین پورت‌ها
- ❌ دو سرور Next.js

### الان (جدید):
- ✅ یک پورت (3000)
- ✅ NextAuth با HttpOnly cookies
- ✅ Prisma + SQLite
- ✅ یک سرور Next.js
- ✅ Route-based separation

# وضعیت پورت‌ها - Port Status

## 🎯 وضعیت فعلی (Current Status)

**فقط یک پورت کار می‌کند: پورت 3000**

### ✅ نحوه کار:

1. **یک سرور Next.js** روی پورت **3000** اجرا می‌شود
2. **ادمین و کاربر** از طریق **route ها** جدا می‌شوند (نه پورت‌ها)
3. **Middleware** دسترسی را بر اساس **role** کنترل می‌کند

## 🚀 نحوه اجرا:

```bash
# فقط یک دستور:
npm run dev
```

سرور روی `http://localhost:3000` اجرا می‌شود.

## 📍 آدرس‌ها:

### پنل کاربران (User Panel):
- **صفحه اصلی**: `http://localhost:3000/`
- **داشبورد**: `http://localhost:3000/dashboard`
- **ثبت نام**: `http://localhost:3000/auth/register`
- **ورود**: `http://localhost:3000/auth/login`

### پنل ادمین (Admin Panel):
- **ورود ادمین**: `http://localhost:3000/admin/login`
- **داشبورد ادمین**: `http://localhost:3000/admin`
- **مدیریت کاربران**: `http://localhost:3000/admin/users`
- **گیفت کارت‌ها**: `http://localhost:3000/admin/gift-cards`

## 🔐 اطلاعات ادمین:

- **Email**: `admin@example.com`
- **Password**: `Admin@12345`

## 🔒 کنترل دسترسی:

### Middleware (`middleware.ts`):
- اگر کاربر **admin** باشد → می‌تواند به `/admin/*` دسترسی داشته باشد
- اگر کاربر **عادی** باشد → به `/admin/*` دسترسی ندارد (redirect به `/dashboard`)
- اگر **لاگین نکرده** باشد → به `/admin/*` redirect می‌شود به `/admin/login`

## ❌ فایل‌های قدیمی (دیگر استفاده نمی‌شوند):

این فایل‌ها مربوط به سیستم قدیمی (دو پورت جداگانه) هستند و می‌توانید حذف کنید:

- `start-admin-port.js` - برای اجرای پورت 3001
- `admin-proxy.js` - Proxy برای دو پورت
- `start-admin.sh` - Script قدیمی
- `README_ADMIN.md` - راهنمای قدیمی (باید به‌روزرسانی شود)

## 📝 تغییرات:

### قبل (قدیمی):
- پورت 3000: پنل کاربران
- پورت 3001: پنل ادمین
- دو سرور Next.js جداگانه
- مشکل sync بین پورت‌ها

### الان (جدید):
- پورت 3000: همه چیز
- Route-based separation
- یک سرور Next.js
- NextAuth برای session management
- Prisma برای database

## 🎯 مزایای سیستم جدید:

1. ✅ **ساده‌تر**: فقط یک سرور
2. ✅ **امن‌تر**: NextAuth با HttpOnly cookies
3. ✅ **قابل اعتمادتر**: Database-backed (SQLite)
4. ✅ **بدون مشکل sync**: همه چیز در یک سرور
5. ✅ **Production-ready**: آماده برای deployment

## 🛠️ دستورات:

```bash
# اجرای سرور
npm run dev

# Build برای production
npm run build

# اجرای production
npm run start

# دیتابیس
npm run db:generate  # Generate Prisma Client
npm run db:migrate   # Run migrations
npm run db:seed      # Seed admin user
npm run db:studio    # Open Prisma Studio
```

## ⚠️ نکات مهم:

1. **فقط پورت 3000**: دیگر نیازی به پورت 3001 نیست
2. **Route-based**: ادمین و کاربر از طریق `/admin` و `/dashboard` جدا می‌شوند
3. **Session-based**: احراز هویت با NextAuth و cookies
4. **Database**: همه کاربران در SQLite database ذخیره می‌شوند



